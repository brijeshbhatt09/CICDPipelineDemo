name: Android build

on: push

jobs:

  build:

    runs-on: ubuntu-latest

    steps:

      - uses: actions/checkout@v1


      - name: Set Up JDK
        uses: actions/setup-java@v1
        with:
          java-version: 1.8

      - name: Clean
        run: ./gradlew clean

      - name: Compile
        run: ./gradlew compileDebugSources

      - name: Unit Tests
        run: ./gradlew test

      - name: Build Project
        run: ./gradlew assemble

      - name: Build Analysis
        run: ./gradlew lint

      - name: Artifact Build report
        if: ${{ always() }}
        uses: actions/upload-artifact@v1
        with:
          name: CICDArtifacts
          path: app/build/reports

      - name: Artifacts APK
        uses: actions/upload-artifact@v1
        with:
         name: CICDArtifacts
         path: app/build/outputs/apk/release/app-release.apk  
      - name: upload app to pCloudy
        env:
          PCLOUDY_USERNAME: ${{ secrets.PCLOUDY_USERNAME }}
          PCLOUDY_ACCESS_KEY: ${{ secrets.PCLOUDY_ACCESS_KEY }}
        run: |
           echo "install cURL"
           
           sudo apt install curl
           echo "Verify install of curl on Ubuntu"
           curl --version     
           echo "upload apk"
           curl -X POST -F "file=app/build/outputs/apk/release/app-release.apk" -F "source_type=raw" -F "token=4by2g5x8q4j7r2pdybythhgb" -F "filter=all" https://device.pcloudy.com/api/upload_file

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2 #checkout the code
        with:
         repository: abhi8130/YovoAndroidAutomation
      - name: upload app to pCloudy
        env:
          PCLOUDY_USERNAME: ${{ secrets.PCLOUDY_USERNAME }}
          PCLOUDY_ACCESS_KEY: ${{ secrets.PCLOUDY_ACCESS_KEY }}
        run: |
           echo "install cURL"
           
           sudo apt install curl
           echo "Verify install of curl on Ubuntu"
           curl --version     
           echo "upload apk"
           curl -X POST -F "file=abhi8130/YovoAndroidAutomation/app/quizu.apk" -F "source_type=raw" -F "token=rvx5tkdstfrgwbhsmhcnrps5" -F "filter=all" https://device.pcloudy.com/api/upload_file
